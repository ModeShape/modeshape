CREATE TABLE EL$VIS (
  COL_A VARCHAR2(20) NOT NULL,
  COL@B VARCHAR2(10) NOT NULL,
  COL#C NUMBER(10)
);

CREATE UNIQUE INDEX IDX$A ON EL$VIS (
   COL@B ASC,
   COL#C DESC
 )
 LOGGING
 COMPRESS 4
 NOPARALLEL
 UNUSABLE;

